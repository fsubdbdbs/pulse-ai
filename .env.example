# Pulse AI — zmienne środowiskowe
# Skopiuj jako .env lokalnie; te same wartości ustaw w panelu Vercela (Settings → Environment Variables)

# --- Logowanie PIN ---
APP_PIN=2137
APP_SECRET=zmien-na-dlug-losowy-ciag-znakow

# --- Bezpieczeństwo crona (VPS → Vercel) ---
CRON_SECRET=zmien-na-inny-losowy-ciag

# --- Push notifications (te same klucze co w opaska — python app/generate_vapid.py) ---
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:zgnilyziemniak123@gmail.com

# --- Vercel KV (ustawia się automatycznie po dodaniu KV Store w panelu Vercela) ---
KV_REST_API_URL=
KV_REST_API_TOKEN=
